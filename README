Dependencies:

  Native Client SDK platform 15+
  Python 3.1+                       (for generating bytecode)

Build instructions:

  echo SDK=/path/to/nacl_sdk >  config.mk
  echo PLATFORM=pepper_15    >> config.mk   (default is pepper_16)
  ./bootstrap.sh
  make

